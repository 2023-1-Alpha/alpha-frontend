// TODO(Changyeop)
export function SeniorBigButton() {
  const [userType, setUserType] = useState('');

  const navigate = useNavigate();

  return (
  );
}

export function SeniorSmallButton() {
    const [userType, setUserType] = useState('');
  
    const navigate = useNavigate();
  
    return (
    );
  }
